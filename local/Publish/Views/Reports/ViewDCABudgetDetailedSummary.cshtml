
@model List<EsselTestLocalApplication.Models.BudgetReportDCASummary>
@if (Model.Count == 0)
{
    <div style="text-align:center;font-weight:600">No Data Found</div>
    @*<script type="text/javascript">
            alert("Model empty");
        </script>*@
}
else
{
    <div class="table-responsive col-md-10">
        <table class="hover stripe nowrap example dataTable no-footer" id="tblIndividualDcaDetails">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Description</th>
                    <th>Credit</th>
                    <th>CashDebit</th>

                    <th>indent</th>
                    <th>PO</th>
                    <th>ChequeDebit</th>
                    <th>ICF</th>
                    <th>Total Debit</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td> @Html.DisplayFor(modelItem => item.paydate)</td>
                        <td style="word-wrap: break-word;"> @Html.DisplayFor(modelItem => item.Description)</td>
                        <td> @Html.DisplayFor(modelItem => item.credit)</td>
                        <td> @Html.DisplayFor(modelItem => item.cashdebit)</td>
                        <td> @Html.DisplayFor(modelItem => item.indent)</td>
                        <td> @Html.DisplayFor(modelItem => item.PO)</td>
                        <td> @Html.DisplayFor(modelItem => item.chequedebit)</td>
                        <td> @Html.DisplayFor(modelItem => item.ICF)</td>
                        <td> @Html.DisplayFor(modelItem => item.TotalDebit)</td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
}
<script type="text/javascript">
    $(document).ready(function () {
        var InddcaGrid = null;
        InitializeIndDCABudgetReportGrid();
    });


    function InitializeIndDCABudgetReportGrid() {
        InddcaGrid = $('#tblIndividualDcaDetails').DataTable({
            destroy: true,
            paging: false,
            lengthChange: false,
            searching: false,
            lengthChange: false,
            info: false,
            paging: false,
            language: {
                "emptyTable": "No Data Found"
            },
            
            "initComplete": function (settings, json) {

            },
            "createdRow": function (row, data, index) {
                //var date = $('td:eq(0)', row).html();
                //var trimStr = $.trim(date);
                //var typeArr = trimStr.split(' ');
                //var d3 = typeArr[0];
                //var t1 = "'" + d3 + "'";
                //var bldate1 = moment(t1, "DDMMYYYY").format('DD-MMM-YYYY');
                //$('td:eq(0)', row).html(bldate1);
                //alert(date);
            },
        });
    }

</script>
